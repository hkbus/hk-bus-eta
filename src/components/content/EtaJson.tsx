import React, { useContext } from "react";
import AppContext from "../../AppContext";

const EtaJson = () => {
  const { data } = useContext(AppContext);

  return <>data</>;
};

export default EtaJson;
